import { useEffect,useMemo,useState } from "react";import { LockKeyhole,Plus,RefreshCw,ShieldCheck,Trash2,UserCog } from "lucide-react";import { createUser,deleteUser,listAudit,listUsers,updateUser,type AdminUser,type AuditEntry } from "../services/adminApi";
const initial={email:"",password:"",role:"user",status:"active",expires_at:"",force_password_change:true};
export default function AdminPage(){const[users,setUsers]=useState<AdminUser[]>([]);const[audit,setAudit]=useState<AuditEntry[]>([]);const[form,setForm]=useState(initial);const[tab,setTab]=useState<"users"|"audit">("users");const[busy,setBusy]=useState(false);const[error,setError]=useState("");const stats=useMemo(()=>({total:users.length,active:users.filter(u=>u.status==="active").length,locked:users.filter(u=>u.status==="locked").length,admins:users.filter(u=>u.role==="admin").length}),[users]);async function load(){setBusy(true);setError("");try{const[nextUsers,nextAudit]=await Promise.all([listUsers(),listAudit()]);setUsers(nextUsers);setAudit(nextAudit);}catch(reason){setError(reason instanceof Error?reason.message:"Could not load administration data.");}finally{setBusy(false);}}useEffect(()=>{void load();},[]);async function add(event:React.FormEvent){event.preventDefault();setBusy(true);setError("");try{await createUser({...form,expires_at:form.expires_at?new Date(form.expires_at).toISOString():null});setForm(initial);await load();}catch(reason){setError(reason instanceof Error?reason.message:"Could not create the account.");}finally{setBusy(false);}}async function change(user:AdminUser,field:"role"|"status",value:string){setBusy(true);try{await updateUser(user.user_id,{[field]:value});await load();}catch(reason){setError(reason instanceof Error?reason.message:"Could not update the account.");}finally{setBusy(false);}}async function remove(user:AdminUser){if(!window.confirm(`Delete ${user.email}? This action cannot be undone.`))return;setBusy(true);try{await deleteUser(user.user_id);await load();}catch(reason){setError(reason instanceof Error?reason.message:"Could not delete the account.");}finally{setBusy(false);}}
return <section className="admin-page"><div className="page-heading"><div><span className="eyebrow">SYSTEM ADMINISTRATION</span><h2>Account management</h2><p>Manage roles and access to Loca Editor.</p></div><button className="secondary-action" onClick={()=>void load()} disabled={busy}><RefreshCw className={busy?"spin":""}/> Refresh</button></div>{error&&<div className="notice error">{error}</div>}<div className="admin-stats"><div><UserCog/><span><strong>{stats.total}</strong><small>Total accounts</small></span></div><div><ShieldCheck/><span><strong>{stats.active}</strong><small>Active</small></span></div><div><LockKeyhole/><span><strong>{stats.locked}</strong><small>Locked</small></span></div><div><ShieldCheck/><span><strong>{stats.admins}</strong><small>Administrators</small></span></div></div><div className="admin-tabs"><button className={tab==="users"?"active":""} onClick={()=>setTab("users")}>Accounts</button><button className={tab==="audit"?"active":""} onClick={()=>setTab("audit")}>Audit log</button></div>{tab==="users"?<div className="admin-grid"><form className="card admin-create" onSubmit={add}><div className="card-title compact"><span className="soft-icon"><Plus/></span><div><h3>Create account</h3><p>The temporary password must be changed after the first sign-in.</p></div></div><label className="field"><span>Email</span><input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label className="field"><span>Temporary password</span><input type="password" minLength={8} required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label><div className="form-grid"><label className="field"><span>Role</span><select value={form.role} onChange={e=>setForm({...form,role:e.target.value})}><option value="user">User</option><option value="agent">Agent</option><option value="admin">Admin</option></select></label><label className="field"><span>Status</span><select value={form.status} onChange={e=>setForm({...form,status:e.target.value})}><option value="active">Active</option><option value="locked">Locked</option><option value="expired">Expired</option></select></label></div><label className="field"><span>Expiration date (optional)</span><input type="datetime-local" value={form.expires_at} onChange={e=>setForm({...form,expires_at:e.target.value})}/></label><label className="admin-check"><input type="checkbox" checked={form.force_password_change} onChange={e=>setForm({...form,force_password_change:e.target.checked})}/> Require password change at first sign-in</label><button className="primary" disabled={busy}><Plus/> Create account</button></form><div className="card admin-list"><h3>Account list</h3><div className="admin-table-wrap"><table><thead><tr><th>Email</th><th>Role</th><th>Status</th><th>Expires</th><th></th></tr></thead><tbody>{users.map(user=><tr key={user.user_id}><td><strong>{user.email}</strong><small>{user.force_password_change?"Password change required":"Password confirmed"}</small></td><td><select value={user.role} disabled={busy} onChange={e=>void change(user,"role",e.target.value)}><option value="admin">Admin</option><option value="agent">Agent</option><option value="user">User</option></select></td><td><select value={user.status} disabled={busy} onChange={e=>void change(user,"status",e.target.value)}><option value="active">Active</option><option value="locked">Locked</option><option value="expired">Expired</option></select></td><td>{user.expires_at?new Date(user.expires_at).toLocaleString("en-US"):"Unlimited"}</td><td><button className="danger-icon" onClick={()=>void remove(user)} aria-label={`Delete ${user.email}`}><Trash2/></button></td></tr>)}</tbody></table></div></div></div>:<div className="card audit-list"><table><thead><tr><th>Time</th><th>Action</th><th>Performed by</th><th>Target account</th></tr></thead><tbody>{audit.map(entry=><tr key={entry.id}><td>{new Date(entry.created_at).toLocaleString("en-US")}</td><td><strong>{entry.action}</strong></td><td>{entry.admin_user_id||"System"}</td><td>{entry.target_user_id||"—"}</td></tr>)}</tbody></table></div>}</section>;}
