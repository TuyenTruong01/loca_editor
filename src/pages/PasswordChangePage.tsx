import { useState } from "react";import { KeyRound,LoaderCircle } from "lucide-react";import { useAuth } from "../contexts/AuthContext";import { markPasswordChanged } from "../services/adminApi";import { isPasswordRecovery,saveSession,updatePassword } from "../services/supabaseAuth";
export default function PasswordChangePage(){const{token}=useAuth();const[password,setPassword]=useState("");const[confirm,setConfirm]=useState("");const[busy,setBusy]=useState(false);const[error,setError]=useState("");async function submit(event:React.FormEvent){event.preventDefault();if(password.length<8)return setError("Password must contain at least 8 characters.");if(password!==confirm)return setError("Passwords do not match.");setBusy(true);setError("");try{const accessToken=await token();if(!accessToken)throw new Error("Your session has expired.");if(!isPasswordRecovery())await markPasswordChanged();await updatePassword(accessToken,password);saveSession(null);window.location.replace("/");}catch(reason){setError(reason instanceof Error?reason.message:"Could not change the password.");}finally{setBusy(false);}}return <main className="login-screen"><section className="login-card"><span className="soft-icon"><KeyRound/></span><div className="login-copy"><span className="eyebrow">ACCOUNT SECURITY</span><h2>Change your initial password</h2><p>Create a personal password before using Loca Editor.</p></div><form onSubmit={submit}><label className="field"><span>New password</span><input type="password" autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><label className="field"><span>Confirm password</span><input type="password" autoComplete="new-password" value={confirm} onChange={e=>setConfirm(e.target.value)} required/></label>{error&&<div className="notice error">{error}</div>}<button className="primary login-submit" disabled={busy}>{busy&&<LoaderCircle className="spin"/>}{busy?"Updating...":"Update password"}</button></form></section></main>;}
